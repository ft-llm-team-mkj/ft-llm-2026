# NeMo-RL dependencies
# Install NeMo-RL following official instructions:
# https://github.com/NVIDIA-NeMo/RL
#
# Clone and install:
#   git clone git@github.com:NVIDIA-NeMo/RL.git nemo-rl --recursive
#   cd nemo-rl
#   uv venv
#   uv pip install -e .
#
# Or install from git directly:
#   uv pip install git+https://github.com/NVIDIA-NeMo/RL.git

# Core dependencies (these are installed with NeMo-RL)
torch>=2.2.0
transformers>=4.46.0
datasets>=3.0.0
ray>=2.9.0
omegaconf>=2.3.0
wandb>=0.23.0

# Additional utilities
accelerate>=0.34.0
